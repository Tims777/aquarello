# GenAI Integration

The GenAI Integration is realized using an external instance of [ComfyUI](https://github.com/Comfy-Org/ComfyUI/). Please refer to the corresponding repository for installation instructions.

For full compatibility, make sure to start ComfyUI using `--preview-method auto` and with a matching CORS configuration, e.g. `--enable-cors-header "*"`.
